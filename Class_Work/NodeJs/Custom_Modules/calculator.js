function add(a, b) {
  return a + b;
}

function sub(a, b) {
  return a - b;
}

function mul(a, b) {
  return a * b;
}

function div(a, b) {
  return a / b;
}

// export { add, sub, mul, div };

const operations = { add, sub, mul, div };

export default operations; 
//
